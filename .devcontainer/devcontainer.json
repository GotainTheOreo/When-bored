{
  "name": "rigging-register",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "forwardPorts": [8000, 5432],
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y git-lfs && git lfs install && pip install -r requirements.txt"
}
